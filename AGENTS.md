# Repository Guidelines & Automation

- **Remote Repository**: `https://github.com/may1102-1909/TARA` (origin main)
- **Automatic Sync**: After making changes in each conversation, automatically stage (`git add .`), commit with a descriptive message, and push directly to `origin main`.
